[package]
name = "dissect"
version = "0.1.0"
authors = ["The Tremor Team"]
edition = "2018"
license = "Apache-2.0"
description = "Logstash inspired dissect extractor"

[dependencies]
halfbrown = "0.1"
simd-json = {version = "0.3", features = ["known-key"]}

[patch.crates-io]
simd-json = { git = "https://github.com/simd-lite/simdjson-rs", branch = "trait-improvements" }
